# Copia este archivo a ".env" (en Backend/api/) para desarrollo local, o
# configura estas mismas variables como "Environment Variables" en Render/Railway.
# NUNCA commitees el ".env" real (ver .gitignore en la raíz del proyecto).

SUPABASE_URL=https://swgqlrbztvqikkyitqtx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN3Z3FscmJ6dHZxaWtreWl0cXR4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI5OTI3MTAsImV4cCI6MjA5ODU2ODcxMH0.MsZZtWDFKUoNNFIipiqAushqXyLDf64HC-4Cv5gGli0

# Sácalo de Supabase Dashboard -> Settings -> API -> JWT Settings -> "JWT Secret".
# Si lo dejas vacío, la API verifica el token llamando a Supabase por red (más lento
# pero funciona igual) en vez de verificarlo localmente.
SUPABASE_JWT_SECRET=

# Dominio(s) del frontend permitidos por CORS, separados por coma.
# En local: http://localhost:3000 . En producción: tu dominio de Vercel.
FRONTEND_ORIGINS=http://localhost:3000
