# Dependencias SOLO de la API (subset del requirements.txt raíz, para un
# despliegue más liviano en Render/Railway — no necesita lightgbm/optuna/
# matplotlib/jupyter, que son cosas de entrenamiento, no de inferencia).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-dotenv>=1.0
joblib>=1.4
pandas>=2.2
numpy>=1.26
# Fijado exacto (no >=1.5): los modelos .joblib se entrenaron y serializaron
# con esta versión — una distinta da InconsistentVersionWarning y arriesga
# resultados ligeramente distintos. Ver requirements.txt raíz para el detalle.
scikit-learn==1.7.2
xgboost==2.1.4
shap>=0.46
